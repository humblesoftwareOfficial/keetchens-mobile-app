import { StyleSheet } from "react-native";
import { APP_COLORS } from "./system";

export const TEAM_LIST_STYLE = StyleSheet.create({
  parent: {
    flex: 1,
    borderRadius: 15,
    marginTop: 10,
  },
  container: {
    backgroundColor: APP_COLORS.WHITE_COLOR.color,
    borderRadius: 15,
  },
});

export const ARTICLE_LIST_STYLE = StyleSheet.create({
  parent: {
    flex: 1,
    borderRadius: 15,
    marginTop: 10,
  },
  container: {
  },
});
